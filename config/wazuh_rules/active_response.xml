<group name="active_response,">

  <!-- ============================================================ -->
  <!-- Active Response: Auto-block IP on brute force                -->
  <!-- ============================================================ -->

  <rule id="100100" level="12">
    <if_sid>100010,100011</if_sid>
    <description>SSH brute force: triggering auto-block active response</description>
    <group>active_response,brute_force</group>
  </rule>

  <rule id="100101" level="12">
    <if_sid>100020</if_sid>
    <description>Port scan detected: triggering auto-block active response</description>
    <group>active_response,port_scan</group>
  </rule>

</group>
